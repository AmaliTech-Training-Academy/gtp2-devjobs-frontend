export interface SeekerProfile {
  fullname: string;
  phoneNumber: number;
  email: string;
  location: string;
  image: File;
}

export interface EmployerProfile {
  companyName: string;
  location: string;
  website: string;
  email: string;
  about: string;
  companySize: string;
  number: string;
}
